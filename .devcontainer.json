{
    "image":"mcr.microsoft.com/devcontainers/go:1.19",
    "mounts": [
        "source=devpod-provider-aws--home,target=/home/vscode,type=volume",
        "source=${localEnv:HOME}/.aws,target=/home/vscode/.aws,type=bind,consistency=cached"
    ]
}